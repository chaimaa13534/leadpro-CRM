export interface ReportQuery { from: string; to: string; userId?: number }
export interface ReportOverview { totalLeads:number;totalContacts:number;totalCompanies:number;totalOpportunities:number;openOpportunities:number;wonOpportunities:number;lostOpportunities:number;estimatedPipelineValue:number;estimatedWonValue:number;winRate:number;completedTasks:number;pendingTasks:number;overdueTasks:number; leadsByStatus:Array<{label:string;value:number}>; leadsBySource:Array<{label:string;value:number}>; leadsOverTime:Array<{label:string;value:number}>; opportunitiesByStatus:Array<{label:string;value:number}>; pipelineByStage:Array<{label:string;count:number;value:number}>; tasksByStatus:Array<{label:string;value:number}>; tasksByPriority:Array<{label:string;value:number}>; performance:Array<{id:number;name:string;leads:number;opportunities:number;won:number;lost:number;estimatedPipelineValue:number;estimatedWonValue:number;completedTasks:number}> }
