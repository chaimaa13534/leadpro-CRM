import { ApiError } from '../utils/api-error.js';import * as repo from '../repositories/notification.repository.js';import type { NotificationQuery } from '../types/notification.types.js';export const getNotifications=async(u:number,q:NotificationQuery)=>{const r=await repo.list(u,q);return{...r,page:q.page,limit:q.limit,totalPages:Math.ceil(r.total/q.limit)}};export const getNotification=async(u:number,id:number)=>{const x=await repo.find(u,id);if(!x)throw ApiError.notFound('Notification not found');return x};export const getUnreadCount=(u:number)=>repo.unreadCount(u).then(count=>({count}));export const markAsRead=async(u:number,id:number)=>{await getNotification(u,id);await repo.markRead(u,id);return getNotification(u,id)};export const markAllAsRead=(u:number)=>repo.markAllRead(u).then(()=>({message:'Notifications marked as read'}));export const deleteNotification=async(u:number,id:number)=>{await getNotification(u,id);await repo.remove(u,id)};export const createNotification=repo.create;
