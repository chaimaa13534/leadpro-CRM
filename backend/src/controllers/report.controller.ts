import type { Request,Response } from 'express';import { asyncHandler } from '../utils/async-handler.js';import { getOverview } from '../services/report.service.js';export const overview=asyncHandler(async(req:Request,res:Response)=>{const now=new Date();const from=typeof req.query.from==='string'?req.query.from:new Date(now.getFullYear(),now.getMonth(),1).toISOString().slice(0,10);const to=typeof req.query.to==='string'?req.query.to:now.toISOString().slice(0,10);const userId=typeof req.query.userId==='string'?Number(req.query.userId):undefined;res.json({success:true,data:await getOverview({from,to,userId},req.user!)})});
