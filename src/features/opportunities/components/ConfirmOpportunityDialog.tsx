import { Modal } from '@/components/ui/Modal'; import { Button } from '@/components/ui/Button';
export function ConfirmOpportunityDialog({ open, onClose, onConfirm, isLoading }: { open: boolean; onClose: () => void; onConfirm: () => void; isLoading: boolean }) { return <Modal open={open} onClose={onClose} title="Supprimer cette opportunité ?" size="sm"><div className="flex flex-col gap-5"><p className="text-[13px] text-text-tertiary">Cette opportunité sera supprimée de façon réversible.</p><div className="flex justify-end gap-2 border-t border-border pt-4"><Button variant="ghost" onClick={onClose} disabled={isLoading}>Annuler</Button><Button variant="danger" loading={isLoading} onClick={onConfirm}>Supprimer</Button></div></div></Modal> }
