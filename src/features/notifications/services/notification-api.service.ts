const KEY='leadpro-crm:session';type Envelope<T>={success:true;data:T};function headers():Record<string,string>{try{const token=(JSON.parse(localStorage.getItem(KEY)??'{}')as{accessToken?:string}).accessToken;return token?{Authorization:`Bearer ${token}`}:{}}catch{return{}}}async function request<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(path,{...init,headers:{'Content-Type':'application/json',...headers(),...(init?.headers??{})}});const b=await r.json()as Envelope<T>;if(!r.ok||!b.success)throw new Error('Impossible de charger les notifications.');return b.data}export interface ApiNotification{id:number;type:string;title:string;message:string;entityType:string|null;entityId:number|null;isRead:boolean;readAt:string|null;createdAt:string}export const listNotifications=()=>request<{items:ApiNotification[];total:number}>('/api/notifications?limit=20');export const unreadCount=()=>request<{count:number}>('/api/notifications/unread-count');export const markRead=(id:number)=>request<ApiNotification>(`/api/notifications/${id}/read`,{method:'PATCH'});export const markAllRead=()=>request<{message:string}>('/api/notifications/read-all',{method:'PATCH'});export const deleteNotification=(id:number)=>request<{message:string}>(`/api/notifications/${id}`,{method:'DELETE'});
