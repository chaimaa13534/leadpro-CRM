import { Modal } from '@/components/ui/Modal';import { Button } from '@/components/ui/Button';export function ConfirmTaskDialog({open,onClose,onConfirm,isLoading}:{open:boolean;onClose:()=>void;onConfirm:()=>void;isLoading:boolean}){return <Modal open={open} onClose={onClose} title="Supprimer cette tâche ?" size="sm"><div className="flex flex-col gap-5"><p className="text-[13px] text-text-tertiary">La tâche sera supprimée de façon réversible.</p><div className="flex justify-end gap-2"><Button variant="ghost" onClick={onClose}>Annuler</Button><Button variant="danger" loading={isLoading} onClick={onConfirm}>Supprimer</Button></div></div></Modal>}
