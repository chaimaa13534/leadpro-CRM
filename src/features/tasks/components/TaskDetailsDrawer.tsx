import { Modal } from '@/components/ui/Modal'; import type { ManagedTask } from '../types/task-management.types'; export function TaskDetailsDrawer({open,task,onClose}:{open:boolean;task:ManagedTask|null;onClose:()=>void}){if(!task)return null;const rows=[['Description',task.description],['Assignée à',`${task.assignee.firstName} ${task.assignee.lastName}`],['Entreprise',task.company?.name],['Contact',task.contact?`${task.contact.firstName} ${task.contact.lastName}`:null],['Priorité',task.priority],['Statut',task.status],['Échéance',task.dueDate],['Créée le',task.createdAt],['Mise à jour',task.updatedAt]];return <Modal open={open} onClose={onClose} title={task.title} size="lg"><dl className="divide-y divide-border rounded-xl border border-border px-4">{rows.map(([l,v])=><div key={l} className="flex justify-between gap-4 py-3 text-[13px]"><dt className="text-text-tertiary">{l}</dt><dd className="text-right font-medium text-text-primary">{v||'Non renseigné'}</dd></div>)}</dl></Modal>}
