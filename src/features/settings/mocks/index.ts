/* ═════════════════════════════════════════════════════════════════════
   Settings & Administration — Mocks (barrel)
   ═════════════════════════════════════════════════════════════════════ */

export {
  defaultGeneralPreferences,
  profileMock,
  accountInfoMock,
  defaultAppearancePreferences,
} from '@/features/settings/mocks/profile.mock';

export { organizationMock } from '@/features/settings/mocks/organization.mock';

export { teamMock } from '@/features/settings/mocks/team.mock';

export {
  rolesMock,
  permissionModules,
  permissionActions,
  departmentsMock,
} from '@/features/settings/mocks/roles.mock';

export { sessionsMock } from '@/features/settings/mocks/sessions.mock';

export {
  twoFactorStatusMock,
  securityActivityMock,
} from '@/features/settings/mocks/security-activity.mock';

export { integrationsMock } from '@/features/settings/mocks/integrations.mock';

export {
  plansMock,
  currentPlanMock,
  planUsageMock,
  invoicesMock,
} from '@/features/settings/mocks/invoices.mock';

export {
  defaultNotificationPreferences,
  defaultAdvancedOptions,
  defaultDataPrivacyPreferences,
} from '@/features/settings/mocks/defaults.mock';

