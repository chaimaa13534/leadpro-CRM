/**
 * Tailles de pagination utilisées par toutes les listes de l'application
 * (leads, contacts, entreprises, opportunités…).
 */
export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_SIZE = 10;

/** Choix proposés dans les sélecteurs "éléments par page". */
export const PAGE_SIZE_OPTIONS: number[] = [10, 25, 50, 100];

export const MAX_PAGE_SIZE = 100;
